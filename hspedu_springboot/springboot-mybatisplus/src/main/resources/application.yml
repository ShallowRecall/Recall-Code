server:
  port: 9090
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot_mybatisplus?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: QY1989424334

mybatis-plus:
  configuration: #进行mybatis-plus配置，配置项和mybatis是一样
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
